<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:background="@color/colorPrimaryDark"
    android:orientation="vertical"
    android:padding="16dp"
    tools:context=".app.LoginActivity">

    <!--<ImageView-->
        <!--android:background="@drawable/loagingtela"-->
        <!--android:layout_width="200dp"-->
        <!--android:layout_marginBottom="30dp"-->
        <!--android:layout_height="150dp" />-->

    <EditText
        android:id="@+id/cpf_login_id"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="2dp"
        android:background="#ecf4fc"
        android:hint="CPF"
        android:textColorHint="@color/colorPrimary"
        android:inputType="number"
        android:padding="10dp"
        android:textSize="16sp" />

    <EditText
        android:id="@+id/senha_login_id"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:background="#ecf4fc"
        android:hint="Senha "
        android:textColorHint="@color/colorPrimary"
        android:inputType="textPassword"
        android:padding="10dp"
        android:textSize="16sp" />

    <Button
        android:id="@+id/acessar_login_id"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorButton"
        android:text="Acessar"
        android:textColor="#fff"
        android:textSize="16sp" />

</LinearLayout>

    <!--android:background="#f4f5f3"-->
